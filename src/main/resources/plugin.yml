name: ${project.name}
version: ${project.version}
main: ${project.mainClass}
author: ${project.author}
permissions:
   autopickup.*:
      default: op
commands:
   autopick:
      usage: /autopick reload
      permission: autopickup.reload